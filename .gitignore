# .gitignore

# Logs and Cache
logs
*.log
npm-debug.log*
yarn-debug.log* # In case yarn is used by other collaborators or future you
yarn-error.log*
pnpm-debug.log* # In case pnpm is used
lerna-debug.log* # Generally for monorepos, but harmless to keep from Vite's default
*.tsbuildinfo    # TypeScript build cache - Important to ignore
.eslintcache     # ESLint cache - Important to ignore

# Dependencies - Never commit this folder
/node_modules

# Build output from Vite - Generated, not versioned
/dist
/dist-ssr

# Environment variables
# Ignore all .env files, but allow an example template to be versioned
.env
.env.*
!.env.example

# Vite specific environment files (often contain local overrides)
*.local

# Test Coverage Reports (Good to have proactively if you plan to add tests)
/coverage
*.lcov
.nyc_output

# Editor directories and files
# VS Code specific (devcontainer.json is primary source for containerized settings)
# These lines help keep the local workspace clean if not using the devcontainer,
# or if specific shared configurations are desired outside the devcontainer spec.
.vscode/*
!.vscode/extensions.json  # Allows sharing recommended extensions for non-devcontainer users
# !.vscode/settings.json # Optional: uncomment if you want to share workspace-specific VS Code settings
# !.vscode/launch.json   # Optional: uncomment if you want to share debug configurations

# JetBrains IDEs (IntelliJ, WebStorm, etc.)
.idea/

# macOS system files
.DS_Store

# Windows system files (thumbnail cache)
Thumbs.db
Desktop.ini # Windows folder customization file

# Legacy Visual Studio / Node.js Tools for Visual Studio (from Vite's default, less common for this stack)
*.suo
*.ntvs*
*.njsproj
*.sln

# Vim swap files
*.sw?
*.swp # More common Vim swap file extension